import React from "react";

/**
 * PUBLIC_INTERFACE
 * AuthGate
 * A simple pass-through component placeholder for future Supabase Auth integration.
 * Wraps children and could later enforce authentication.
 */
export default function AuthGate({ children }) {
  return <>{children}</>;
}
